events {}
http {
    server {
        listen 80;
        root /opt/app;
        index index.html;
        include /etc/nginx/mime.types;
        location / {
            try_files $uri $uri/ /;
        }
        location /api/ {
            proxy_pass http://api:8080/;
        }
    }
}
